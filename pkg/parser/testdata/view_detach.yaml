views:
    - name: daily_summary
      database: analytics
      operation: DETACH
    - name: users_view
      operation: DETACH
      if_exists: true
    - name: stats_view
      cluster: production
      operation: DETACH
    - name: my_view
      database: db
      operation: DETACH
      permanently: true
    - name: updated_view
      database: analytics
      operation: DETACH
      sync: true
    - name: old_view
      cluster: production
      operation: DETACH
      if_exists: true
      permanently: true
      sync: true
tables:
    - name: mv_daily_stats
      operation: DETACH
    - name: mv_aggregated
      database: analytics
      operation: DETACH
    - name: mv_complex
      cluster: production
      operation: DETACH
    - name: mv_joins
      database: analytics
      operation: DETACH
      permanently: true
      sync: true
    - name: mv_old
      database: analytics
      cluster: analytics_cluster
      operation: DETACH
      permanently: true
      sync: true
