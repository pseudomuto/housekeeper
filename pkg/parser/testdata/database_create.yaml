databases:
    - name: basic_db
    - name: conditional_db
    - name: cluster_db
      cluster: my_cluster
    - name: engine_db
      engine: Atomic
    - name: remote_db
      engine: MySQL('localhost:3306', 'database', 'user', 'password')
    - name: materialized_db
      engine: MaterializedMySQL('localhost:3306', 'database', 'user', 'password', 5000)
    - name: comment_db
      comment: This is a test database
    - name: full_db
      cluster: production
      engine: Atomic
      comment: Full featured database
    - name: '`user-database`'
      engine: Atomic
    - name: '`order-db`'
      cluster: '`prod-cluster`'
      comment: Database with special chars
