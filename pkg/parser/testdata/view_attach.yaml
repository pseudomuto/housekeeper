views:
    - name: daily_summary
      database: analytics
      operation: ATTACH
    - name: users_view
      operation: ATTACH
    - name: stats_view
      cluster: production
      operation: ATTACH
    - name: my_view
      database: db
      operation: ATTACH
tables:
    - name: mv_daily_stats
      operation: ATTACH
    - name: mv_aggregated
      database: analytics
      operation: ATTACH
    - name: mv_complex
      cluster: production
      operation: ATTACH
    - name: mv_joins
      database: analytics
      cluster: analytics_cluster
      operation: ATTACH
