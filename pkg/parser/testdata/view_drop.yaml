views:
    - name: daily_summary
      database: analytics
      operation: DROP
    - name: users_view
      operation: DROP
    - name: stats_view
      cluster: production
      operation: DROP
    - name: my_view
      database: db
      operation: DROP
    - name: old_view
      cluster: production
      operation: DROP
tables:
    - name: mv_daily_stats
      operation: DROP
    - name: mv_aggregated
      database: analytics
      operation: DROP
    - name: mv_complex
      cluster: production
      operation: DROP
    - name: mv_joins
      database: analytics
      operation: DROP
      sync: true
    - name: mv_old
      database: analytics
      cluster: analytics_cluster
      operation: DROP
      sync: true
