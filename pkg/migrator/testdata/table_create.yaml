description: "Creating new tables"
current_sql: ""  # Empty current state
target_sql: |
  CREATE TABLE users (
      id UInt64,
      name String,
      email String
  ) ENGINE = MergeTree()
  ORDER BY id;
expected_migration:
  sql_contains:
    - "CREATE TABLE users"
    - "id UInt64"
    - "name String"
    - "email String"
    - "ENGINE = MergeTree"
    - "ORDER BY id"
  diff_count: 1
  diff_types:
    - "CREATE"