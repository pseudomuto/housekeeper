description: "Dropping tables"
current_sql: |
  CREATE TABLE old_table (
      id UInt64,
      data String
  ) ENGINE = MergeTree()
  ORDER BY id;
target_sql: ""  # Empty target state
expected_migration:
  sql_contains:
    - "DROP TABLE old_table"
  diff_count: 1
  diff_types:
    - "DROP"