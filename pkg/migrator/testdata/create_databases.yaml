description: "Creating new databases from empty state"
current_sql: ""  # Empty current state
target_sql: |
  CREATE DATABASE analytics ENGINE = Atomic COMMENT 'Analytics database';
  CREATE DATABASE logs ON CLUSTER production ENGINE = MySQL('localhost:3306', 'logs', 'user', 'pass') COMMENT 'Logs database';
expected_migration:
  up_contains:
    - "CREATE DATABASE analytics ENGINE = Atomic COMMENT 'Analytics database'"
    - "CREATE DATABASE logs ON CLUSTER production ENGINE = MySQL('localhost:3306', 'logs', 'user', 'pass') COMMENT 'Logs database'"
  down_contains:
    - "DROP DATABASE IF EXISTS analytics"
    - "DROP DATABASE IF EXISTS logs ON CLUSTER production"
  diff_count: 2
  diff_types:
    - "CREATE"
    - "CREATE"